import type { FontFamilyValues } from "../../element/types";
interface FontPickerTriggerProps {
    selectedFontFamily: FontFamilyValues | null;
    disabled?: boolean;
}
export declare const FontPickerTrigger: ({ selectedFontFamily, disabled, }: FontPickerTriggerProps) => import("react/jsx-runtime").JSX.Element;
export {};
